# 2026-07-19

## 工程治理第 1 步：stock_data 异常可观测性

- 用户同意在不改变现有功能和内容的前提下逐项治理工程债务；执行原则是每次只处理一个边界，独立测试、独立提交。
- 第一项只处理 `backend/data/stock_data.py` 的外部数据源静默降级，不拆文件、不改算法、不改 API、不改返回结构和前端内容。
- 新增 `utils/fallback_log.py`：使用标准 logging 记录数据源、操作、异常类型、错误和股票/日期等上下文；同类错误 60 秒限频，并在下一条日志附带被抑制次数，避免数据源故障刷屏。
- 已接入主营业务、季度财务、龙虎榜、热点题材、行业横向、资金流、新闻公告、指数、估值、快速复盘、回测历史和形态识别等主要降级点。
- 保留数值转换、baostock logout 和进度回调中的静默兜底，因为这些不改变市场数据语义，也避免非关键清理失败打断主流程。
- 新增 5 项测试，锁定“错误会记录，但返回值仍与原来一致”；全量后端测试从 21 项增加到 26 项，全部通过；前端生产构建通过且产物未变化。
